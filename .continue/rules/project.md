# Project workflow

## Local environment
Project is served locally for development via orbstack using tilt.

Files are sync automatically, upon file change the local script will perform build, copy and will run in a pod.

Domain for the development is `http://goblncr-svc.goblncr.svc.cluster.local:8080/`

