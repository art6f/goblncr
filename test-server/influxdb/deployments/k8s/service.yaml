apiVersion: v1
kind: Service
metadata:
  labels:
    app: influxdb
  name: influxdb
  namespace: goblncr-test
spec:
  ports:
    - name: "web-port"
      port: 80
      targetPort: 8086
  selector:
    app: influxdb
